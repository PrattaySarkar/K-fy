import 'package:flutter/material.dart';

abstract final class AppColors {
  static const Color background = Color(0xFF050505);
  static const Color oledBackground = Color(0xFF000000);
  static const Color card = Color(0xFF121212);
  static const Color surface = Color(0xFF121212);
  static const Color surfaceElevated = Color(0xFF1A1A1A);
  static const Color border = Color(0xFF1E1E1E);
  static const Color textPrimary = Color(0xFFF4F5F7);
  static const Color textSecondary = Color(0xFF9A9A9A);
  static const Color textMuted = Color(0xFF6E6E6E);
  static const Color danger = Color(0xFFE85D5D);
  static const Color dangerSoft = Color(0x33E85D5D);

  /// Flat matte olive — the only accent.
  static const Color accent = Color(0xFF78866B);
  static const Color accentDefault = accent;
  static const Color accentDeep = Color(0xFF5F6B56);
}
